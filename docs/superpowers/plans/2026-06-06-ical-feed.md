# iCal-Feed Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Jeder Teilnehmer kann ein Event als Kalender-Abonnement (Outlook, Apple Calendar, Google Calendar) einbinden; Terminvorschläge erscheinen als TENTATIVE-Einträge mit Abstimmungsstand, der Finaltermin als CONFIRMED; der Organizer kann optional Uhrzeiten setzen und allen Teilnehmern ihren persönlichen Webcal-Link per E-Mail schicken.

**Architecture:** Persönlicher `calendar_token` pro User (DB-Feld, lazy generiert). Öffentlicher `GET /events/{id}/calendar.ics?token=<token>` Endpoint baut iCal aus aktuellen DB-Daten. Frontend ruft `POST /auth/calendar-token` auf und baut `webcal://`-URL clientseitig zusammen.

**Tech Stack:** FastAPI + SQLAlchemy async, Alembic, Celery, Vue 3 Composition API, RFC 5545 (iCalendar)

---

## Dateiübersicht

| Datei | Aktion |
|---|---|
| `backend/migrations/versions/add_calendar_token.py` | Neu (Migration 1) |
| `backend/migrations/versions/add_event_time_fields.py` | Neu (Migration 2) |
| `backend/models.py` | Modify: `calendar_token` + `event_start_time/end_time` |
| `backend/schemas.py` | Modify: `EventPatch` + `EventResponse` |
| `backend/api/auth.py` | Modify: neuer Endpoint `POST /auth/calendar-token` |
| `backend/api/events.py` | Modify: 2 neue Endpoints + Helper-Funktion |
| `backend/tasks.py` | Modify: neuer Celery-Task |
| `frontend/src/views/EventDetails.vue` | Modify: State + Template |

---

### Task 1: Alembic-Migrationen erstellen und ausführen

**Files:**
- Create: `backend/migrations/versions/add_calendar_token.py`
- Create: `backend/migrations/versions/add_event_time_fields.py`

- [ ] **Schritt 1: Migration 1 anlegen — calendar_token**

```bash
docker exec -it eventfinder-backend bash -c "cd /app && alembic revision --autogenerate -m 'add_calendar_token'"
```

Danach die erzeugte Datei in `migrations/versions/` öffnen und sicherstellen dass sie so aussieht:

```python
def upgrade() -> None:
    op.add_column('users', sa.Column('calendar_token', sa.String(64), nullable=True))
    op.create_unique_constraint('uq_users_calendar_token', 'users', ['calendar_token'])

def downgrade() -> None:
    op.drop_constraint('uq_users_calendar_token', 'users', type_='unique')
    op.drop_column('users', 'calendar_token')
```

- [ ] **Schritt 2: Migration 2 anlegen — event time fields**

```bash
docker exec -it eventfinder-backend bash -c "cd /app && alembic revision --autogenerate -m 'add_event_time_fields'"
```

Erzeugte Datei prüfen/anpassen:

```python
def upgrade() -> None:
    op.add_column('events', sa.Column('event_start_time', sa.String(5), nullable=True))
    op.add_column('events', sa.Column('event_end_time', sa.String(5), nullable=True))

def downgrade() -> None:
    op.drop_column('events', 'event_end_time')
    op.drop_column('events', 'event_start_time')
```

- [ ] **Schritt 3: Beide Migrationen ausführen**

```bash
docker exec -it eventfinder-backend bash -c "cd /app && alembic upgrade head"
```

Erwartete Ausgabe:
```
Running upgrade ... -> add_calendar_token
Running upgrade ... -> add_event_time_fields
```

- [ ] **Schritt 4: Spalten prüfen**

```bash
docker exec -it eventfinder-db psql -U postgres -d eventfinder -c "\d users" | grep calendar_token
docker exec -it eventfinder-db psql -U postgres -d eventfinder -c "\d events" | grep event_start
```

Erwartete Ausgabe: Beide Spalten sichtbar.

- [ ] **Schritt 5: Commit**

```bash
cd /docker/eventfinder
git add backend/migrations/versions/
git commit -m "feat: add calendar_token and event time field migrations"
```

---

### Task 2: models.py — neue Felder

**Files:**
- Modify: `backend/models.py`

- [ ] **Schritt 1: `calendar_token` zu `User` hinzufügen**

In `backend/models.py`, nach der Zeile `is_active: Mapped[bool]...` einfügen:

```python
    calendar_token: Mapped[Optional[str]] = mapped_column(String(64), unique=True, nullable=True)
```

- [ ] **Schritt 2: `event_start_time` und `event_end_time` zu `Event` hinzufügen**

In `backend/models.py`, nach der Zeile `background_overlay: Mapped[float]...` einfügen:

```python
    event_start_time: Mapped[Optional[str]] = mapped_column(String(5), nullable=True)
    event_end_time: Mapped[Optional[str]] = mapped_column(String(5), nullable=True)
```

- [ ] **Schritt 3: Backend neu starten und prüfen**

```bash
cd /docker/eventfinder && docker compose restart backend worker
sleep 5 && docker logs eventfinder-backend 2>&1 | tail -5
```

Erwartete Ausgabe: Keine Fehler, `Application startup complete`.

- [ ] **Schritt 4: Commit**

```bash
cd /docker/eventfinder
git add backend/models.py
git commit -m "feat: add calendar_token to User, event_start/end_time to Event"
```

---

### Task 3: schemas.py — EventPatch und EventResponse erweitern

**Files:**
- Modify: `backend/schemas.py`

- [ ] **Schritt 1: `EventPatch` erweitern**

In `backend/schemas.py`, in der `EventPatch`-Klasse nach `background_overlay: Optional[float] = None` einfügen:

```python
    event_start_time: Optional[str] = None  # "HH:MM"
    event_end_time: Optional[str] = None    # "HH:MM"
```

- [ ] **Schritt 2: `EventResponse` erweitern**

In `backend/schemas.py`, in der `EventResponse`-Klasse nach `background_overlay: float = 0.55` einfügen:

```python
    event_start_time: Optional[str] = None
    event_end_time: Optional[str] = None
```

- [ ] **Schritt 3: Endpoint-Test**

```bash
# Token aus dem Browser-localStorage holen, dann:
curl -s http://localhost:58416/events/ \
  -H "Authorization: Bearer <dein-jwt>" | python3 -m json.tool | grep event_start
```

Erwartete Ausgabe: `"event_start_time": null` in jedem Event.

- [ ] **Schritt 4: Commit**

```bash
cd /docker/eventfinder
git add backend/schemas.py
git commit -m "feat: add event_start/end_time to EventPatch and EventResponse schemas"
```

---

### Task 4: POST /auth/calendar-token Endpoint

**Files:**
- Modify: `backend/api/auth.py`

- [ ] **Schritt 1: Import ergänzen**

In `backend/api/auth.py` prüfen ob `OAuth2PasswordBearer` bereits importiert ist — falls nicht, zur bestehenden FastAPI-Import-Zeile hinzufügen:

```python
from fastapi.security import OAuth2PasswordBearer
```

Dann direkt nach dem `router = APIRouter()` einfügen:

```python
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="auth/verify")
```

Hinweis: `secrets`, `uuid` und `import auth` sind bereits in der Datei vorhanden — nicht doppelt importieren.

- [ ] **Schritt 2: Endpoint hinzufügen**

Am Ende von `backend/api/auth.py` anfügen:

```python
@router.post("/calendar-token")
async def get_calendar_token(
    token: str = Depends(oauth2_scheme),
    db: AsyncSession = Depends(get_db),
):
    payload = auth.verify_token(token)
    if payload is None:
        raise HTTPException(status_code=401, detail="Invalid token")
    user_id = payload.get("sub")
    result = await db.execute(select(models.User).where(models.User.id == uuid.UUID(user_id)))
    user = result.scalar_one_or_none()
    if user is None:
        raise HTTPException(status_code=401, detail="User not found")

    if not user.calendar_token:
        user.calendar_token = secrets.token_urlsafe(32)
        await db.commit()
        await db.refresh(user)

    return {"calendar_token": user.calendar_token}
```

- [ ] **Schritt 3: Container neu starten**

```bash
cd /docker/eventfinder && docker compose restart backend
sleep 5
```

- [ ] **Schritt 4: Endpoint testen**

```bash
# <TOKEN> = JWT aus dem Browser
curl -s -X POST http://localhost:58416/auth/calendar-token \
  -H "Authorization: Bearer <TOKEN>"
```

Erwartete Ausgabe: `{"calendar_token": "xxxxxx..."}` (64-Zeichen-String).

Zweiter Aufruf liefert denselben Token (nicht neu generiert).

- [ ] **Schritt 5: Commit**

```bash
cd /docker/eventfinder
git add backend/api/auth.py
git commit -m "feat: add POST /auth/calendar-token endpoint with lazy token generation"
```

---

### Task 5: GET /events/{id}/calendar.ics Endpoint

**Files:**
- Modify: `backend/api/events.py`

- [ ] **Schritt 1: Imports ergänzen**

In `backend/api/events.py`, die bestehende Import-Zeile `from datetime import datetime, timedelta` zu:

```python
from datetime import datetime, timedelta, date as date_type
```

und nach den bestehenden FastAPI-Imports ergänzen:

```python
from fastapi.responses import Response
```

- [ ] **Schritt 2: Helper-Funktion hinzufügen**

Direkt nach den Konstanten `ALLOWED_IMAGE_TYPES` und `MAX_IMAGE_BYTES` einfügen:

```python
def _ical_escape(text: str) -> str:
    if not text:
        return ""
    return (
        text.replace("\\", "\\\\")
            .replace(";", "\\;")
            .replace(",", "\\,")
            .replace("\n", "\\n")
    )
```

- [ ] **Schritt 3: iCal-Endpoint hinzufügen**

Am Ende von `backend/api/events.py` (vor dem letzten bestehenden Endpoint oder am Ende) einfügen:

```python
@router.get("/{event_id}/calendar.ics")
async def get_calendar_feed(
    event_id: uuid.UUID,
    token: str,
    db: AsyncSession = Depends(get_db),
):
    # Validiere calendar_token
    result = await db.execute(
        select(models.User).where(models.User.calendar_token == token)
    )
    user = result.scalar_one_or_none()
    if not user:
        raise HTTPException(status_code=401, detail="Invalid calendar token")

    # Prüfe Teilnahme
    part_result = await db.execute(
        select(models.EventParticipant)
        .where(models.EventParticipant.event_id == event_id)
        .where(models.EventParticipant.user_id == user.id)
    )
    if not part_result.scalar_one_or_none():
        raise HTTPException(status_code=403, detail="Not a participant of this event")

    # Event laden
    ev_result = await db.execute(select(models.Event).where(models.Event.id == event_id))
    event = ev_result.scalar_one_or_none()
    if not event:
        raise HTTPException(status_code=404, detail="Event not found")

    # Terminvorschläge laden
    prop_result = await db.execute(
        select(models.DateProposal)
        .where(models.DateProposal.event_id == event_id)
        .order_by(models.DateProposal.proposed_date)
    )
    proposals = prop_result.scalars().all()

    # Teilnehmer + Abstimmungen für DESCRIPTION
    parts_result = await db.execute(
        select(models.User, models.EventParticipant)
        .join(models.EventParticipant, models.User.id == models.EventParticipant.user_id)
        .where(models.EventParticipant.event_id == event_id)
    )
    participants = parts_result.all()

    avail_result = await db.execute(
        select(models.Availability)
        .join(models.EventParticipant, models.EventParticipant.id == models.Availability.participant_id)
        .where(models.EventParticipant.event_id == event_id)
    )
    availabilities = avail_result.scalars().all()

    ep_to_name = {ep.id: u.name for u, ep in participants}

    location = _ical_escape(event.address or event.location_name or "")
    title = _ical_escape(event.title)

    lines = [
        "BEGIN:VCALENDAR",
        "VERSION:2.0",
        "PRODID:-//EventFinder//EventFinder//DE",
        f"X-WR-CALNAME:{title}",
        "REFRESH-INTERVAL;VALUE=DURATION:PT1H",
        "X-PUBLISHED-TTL:PT1H",
    ]

    for p in proposals:
        d = p.proposed_date
        date_str = d.strftime("%Y%m%d")
        next_day = (d + timedelta(days=1)).strftime("%Y%m%d")

        day_avails = [a for a in availabilities if a.event_date == d]
        voted_ep_ids = {a.participant_id for a in day_avails}

        best = [ep_to_name.get(a.participant_id, "?") for a in day_avails if a.status.value == "best"]
        possible = [ep_to_name.get(a.participant_id, "?") for a in day_avails if a.status.value == "possible"]
        impossible = [ep_to_name.get(a.participant_id, "?") for a in day_avails if a.status.value == "impossible"]
        pending = [u.name for u, ep in participants if ep.id not in voted_ep_ids]

        desc_parts = []
        if best:
            desc_parts.append(f"Perfekt: {', '.join(best)}")
        if possible:
            desc_parts.append(f"Moeglich: {', '.join(possible)}")
        if impossible:
            desc_parts.append(f"Nicht moeglich: {', '.join(impossible)}")
        if pending:
            desc_parts.append(f"Ausstehend: {', '.join(pending)}")
        description = _ical_escape("Abstimmung:\n" + "\n".join(desc_parts) if desc_parts else "Noch keine Abstimmungen")

        lines += [
            "BEGIN:VEVENT",
            f"UID:proposal-{date_str}-{event_id}@eventfinder",
            f"DTSTART;VALUE=DATE:{date_str}",
            f"DTEND;VALUE=DATE:{next_day}",
            f"SUMMARY:[Vorschlag] {title}",
            "STATUS:TENTATIVE",
            "TRANSP:TRANSPARENT",
            f"LOCATION:{location}",
            f"DESCRIPTION:{description}",
            "END:VEVENT",
        ]

    if event.final_date:
        d = event.final_date
        date_str = d.strftime("%Y%m%d")

        if event.event_start_time and event.event_end_time:
            start_hhmm = event.event_start_time.replace(":", "") + "00"
            end_hhmm = event.event_end_time.replace(":", "") + "00"
            lines += [
                "BEGIN:VEVENT",
                f"UID:final-{event_id}@eventfinder",
                f"DTSTART;TZID=Europe/Berlin:{date_str}T{start_hhmm}",
                f"DTEND;TZID=Europe/Berlin:{date_str}T{end_hhmm}",
                f"SUMMARY:{title}",
                "STATUS:CONFIRMED",
                "TRANSP:OPAQUE",
                f"LOCATION:{location}",
                "END:VEVENT",
            ]
        else:
            next_day = (d + timedelta(days=1)).strftime("%Y%m%d")
            lines += [
                "BEGIN:VEVENT",
                f"UID:final-{event_id}@eventfinder",
                f"DTSTART;VALUE=DATE:{date_str}",
                f"DTEND;VALUE=DATE:{next_day}",
                f"SUMMARY:{title}",
                "STATUS:CONFIRMED",
                "TRANSP:OPAQUE",
                f"LOCATION:{location}",
                "END:VEVENT",
            ]

    lines.append("END:VCALENDAR")
    ical_content = "\r\n".join(lines) + "\r\n"

    return Response(
        content=ical_content,
        media_type="text/calendar; charset=utf-8",
        headers={"Content-Disposition": 'attachment; filename="event.ics"'},
    )
```

- [ ] **Schritt 4: Backend neu starten**

```bash
cd /docker/eventfinder && docker compose restart backend
sleep 5
```

- [ ] **Schritt 5: Endpoint testen**

```bash
# calendar_token aus Schritt POST /auth/calendar-token, event_id aus der App-URL
curl -v "http://localhost:58416/events/<EVENT_ID>/calendar.ics?token=<CALENDAR_TOKEN>"
```

Erwartete Ausgabe: `BEGIN:VCALENDAR` ... `END:VCALENDAR` mit korrektem Content-Type `text/calendar`.

Falscher Token → HTTP 401. Nicht-Teilnehmer → HTTP 403.

- [ ] **Schritt 6: Commit**

```bash
cd /docker/eventfinder
git add backend/api/events.py
git commit -m "feat: add GET /events/{id}/calendar.ics iCal feed endpoint"
```

---

### Task 6: Celery-Task + POST /events/{id}/send-calendar-links

**Files:**
- Modify: `backend/tasks.py`
- Modify: `backend/api/events.py`

- [ ] **Schritt 1: Celery-Task in tasks.py hinzufügen**

Am Ende von `backend/tasks.py` einfügen:

```python
@celery_app.task
def send_calendar_subscription_emails(event_id: str):
    asyncio.run(_send_calendar_subscription_emails_async(event_id))


async def _send_calendar_subscription_emails_async(event_id: str):
    import sys
    import secrets as secrets_mod
    from urllib.parse import urlparse
    app_dir = os.path.dirname(os.path.abspath(__file__))
    if app_dir not in sys.path:
        sys.path.insert(0, app_dir)
    import models
    from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
    from sqlalchemy.orm import sessionmaker
    from sqlalchemy import select

    engine = create_async_engine(os.getenv("DATABASE_URL"), echo=False)
    async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

    try:
        async with async_session() as session:
            event_result = await session.execute(
                select(models.Event).where(models.Event.id == uuid.UUID(event_id))
            )
            event = event_result.scalar_one_or_none()
            if not event:
                return

            parts_result = await session.execute(
                select(models.User)
                .join(models.EventParticipant, models.User.id == models.EventParticipant.user_id)
                .where(models.EventParticipant.event_id == event.id)
            )
            participants = parts_result.scalars().all()

            settings = _get_smtp_settings()
            base_url = settings.get("frontend_url", "http://localhost:5173").rstrip("/")
            webcal_host = urlparse(base_url).netloc

            for participant in participants:
                if not participant.calendar_token:
                    participant.calendar_token = secrets_mod.token_urlsafe(32)
                    await session.commit()
                    await session.refresh(participant)

                ics_path = f"/events/{event_id}/calendar.ics?token={participant.calendar_token}"
                webcal_url = f"webcal://{webcal_host}{ics_path}"
                https_url = f"{base_url}{ics_path}"

                body = (
                    f"Hallo {participant.name},\n\n"
                    f"Du kannst das Event \"{event.title}\" in deinen Kalender abonnieren.\n"
                    f"Der Kalender aktualisiert sich automatisch, wenn sich Termine aendern.\n\n"
                    f"Klicke hier, um das Abo in deiner Kalender-App zu oeffnen:\n"
                    f"{webcal_url}\n\n"
                    f"Oder trage diese URL manuell ein:\n"
                    f"{https_url}\n\n"
                    f"Anleitung:\n"
                    f"  Google Calendar: Andere Kalender -> Per URL hinzufuegen\n"
                    f"  Outlook: Kalender hinzufuegen -> Aus dem Internet abonnieren\n"
                    f"  Apple Calendar: Ablage -> Kalenderabonnement\n\n"
                    f"Wichtig: Dieser Link ist persoenlich - bitte nicht weitergeben.\n\n"
                    f"-- EventFinder"
                )
                await _send_email(
                    f"Kalender abonnieren: {event.title}",
                    participant.email,
                    body,
                    settings,
                )
    finally:
        await engine.dispose()
```

- [ ] **Schritt 2: Endpoint in api/events.py hinzufügen**

Direkt nach dem `send-invites` Endpoint in `backend/api/events.py` einfügen:

```python
@router.post("/{event_id}/send-calendar-links", status_code=200)
async def send_calendar_links_endpoint(
    event_id: uuid.UUID,
    current_user: models.User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    from tasks import send_calendar_subscription_emails
    event = await _get_event_as_participant(event_id, current_user, db)
    if event.organizer_id != current_user.id:
        raise HTTPException(status_code=403, detail="Only the organizer can send calendar links")
    send_calendar_subscription_emails.delay(str(event_id))
    return {"message": "Kalender-Links werden versendet"}
```

- [ ] **Schritt 3: Backend und Worker neu starten**

```bash
cd /docker/eventfinder && docker compose restart backend worker
sleep 5
```

- [ ] **Schritt 4: Endpoint testen**

```bash
curl -s -X POST "http://localhost:58416/events/<EVENT_ID>/send-calendar-links" \
  -H "Authorization: Bearer <TOKEN>"
```

Erwartete Ausgabe: `{"message": "Kalender-Links werden versendet"}`.

Worker-Logs prüfen:
```bash
docker logs eventfinder-worker 2>&1 | tail -20
```

- [ ] **Schritt 5: Commit**

```bash
cd /docker/eventfinder
git add backend/tasks.py backend/api/events.py
git commit -m "feat: add send_calendar_subscription_emails task and POST /send-calendar-links"
```

---

### Task 7: Frontend — EventDetails.vue

**Files:**
- Modify: `frontend/src/views/EventDetails.vue`

- [ ] **Schritt 1: State-Variablen hinzufügen**

Im `<script setup>` Block, nach der Zeile `const calSent = ref(false)` einfügen:

```typescript
// --- Kalender-Abo ---
const calToken = ref<string | null>(null)
const calTokenLoading = ref(false)
const showCalSubscribe = ref(false)
const calLinkSending = ref(false)
const calLinkSent = ref(false)

// --- Event-Uhrzeit (Organizer) ---
const eventStartTime = ref('')
const eventEndTime = ref('')
const timeSaving = ref(false)
```

- [ ] **Schritt 2: Zeitfelder beim Laden initialisieren**

In der `load()`-Funktion, nach `event.value = evRes.data` einfügen:

```typescript
    eventStartTime.value = event.value?.event_start_time ?? ''
    eventEndTime.value = event.value?.event_end_time ?? ''
```

- [ ] **Schritt 3: Neue Funktionen hinzufügen**

Nach der `sendCalendarInvites()`-Funktion einfügen:

```typescript
async function fetchCalendarToken() {
  calTokenLoading.value = true
  try {
    const res = await axios.post('/auth/calendar-token', {}, { headers: headers() })
    calToken.value = res.data.calendar_token
    showCalSubscribe.value = true
  } catch (e) {
    console.error('Calendar token fetch failed', e)
  } finally {
    calTokenLoading.value = false
  }
}

function calendarWebcalUrl(): string {
  return `webcal://${window.location.host}/events/${eventId}/calendar.ics?token=${calToken.value}`
}

function calendarHttpsUrl(): string {
  return `${window.location.origin}/events/${eventId}/calendar.ics?token=${calToken.value}`
}

async function copyCalendarUrl() {
  try {
    await navigator.clipboard.writeText(calendarHttpsUrl())
  } catch {
    /* Fallback: select text */
  }
}

async function sendCalendarLinks() {
  calLinkSending.value = true
  try {
    await axios.post(`/events/${eventId}/send-calendar-links`, {}, { headers: headers() })
    calLinkSent.value = true
    setTimeout(() => { calLinkSent.value = false }, 3000)
  } catch (e) {
    console.error('Send calendar links failed', e)
  } finally {
    calLinkSending.value = false
  }
}

async function saveEventTime() {
  timeSaving.value = true
  try {
    await axios.patch(`/events/${eventId}`, {
      event_start_time: eventStartTime.value || null,
      event_end_time: eventEndTime.value || null,
    }, { headers: headers() })
    await load()
  } catch (e) {
    console.error('Save event time failed', e)
  } finally {
    timeSaving.value = false
  }
}
```

- [ ] **Schritt 4: "Kalender abonnieren"-Karte in Template einfügen**

Im `<template>`, nach dem Schließen der "Teilnehmer"-Karte (`</div>` nach `</ParticipantList>`, ca. Zeile 668) einfügen:

```html
          <!-- Kalender abonnieren (alle Teilnehmer) -->
          <div style="background:var(--bg-surface); border:1px solid var(--border); border-radius:16px; padding:20px;">
            <p style="font-size:11px; color:rgba(255,255,255,0.35); text-transform:uppercase; letter-spacing:.08em; margin-bottom:12px;">🗓 Kalender abonnieren</p>
            <p style="font-size:13px; color:rgba(255,255,255,0.4); margin-bottom:14px;">Terminvorschläge und Finaltermin erscheinen automatisch in deinem Kalender.</p>

            <div v-if="!showCalSubscribe">
              <button @click="fetchCalendarToken" :disabled="calTokenLoading"
                :style="{ width:'100%', padding:'10px', borderRadius:'12px', border:'none', cursor: calTokenLoading ? 'default' : 'pointer', fontWeight:600, fontSize:'13px', color:'#000', background: event.accent_color, opacity: calTokenLoading ? 0.7 : 1 }">
                {{ calTokenLoading ? 'Lade…' : '🔗 Meine Kalender-URL anzeigen' }}
              </button>
            </div>

            <div v-else>
              <div style="background:rgba(255,255,255,0.04); border:1px solid rgba(255,255,255,0.1); border-radius:10px; padding:10px 12px; font-size:11px; color:rgba(255,255,255,0.5); word-break:break-all; margin-bottom:10px; font-family:monospace;">
                {{ calendarHttpsUrl() }}
              </div>
              <div style="display:flex; gap:8px; margin-bottom:12px;">
                <a :href="calendarWebcalUrl()"
                  :style="{ flex:1, padding:'8px', borderRadius:'10px', border:'none', cursor:'pointer', fontWeight:600, fontSize:'12px', color:'#000', background: event.accent_color, textAlign:'center', textDecoration:'none', display:'flex', alignItems:'center', justifyContent:'center' }">
                  📅 In Kalender öffnen
                </a>
                <button @click="copyCalendarUrl"
                  style="flex:1; padding:8px; border-radius:10px; border:1px solid rgba(255,255,255,0.15); background:transparent; color:rgba(255,255,255,0.7); cursor:pointer; font-size:12px; font-weight:600;">
                  📋 URL kopieren
                </button>
              </div>
              <p style="font-size:11px; color:rgba(255,255,255,0.25); line-height:1.6; margin:0;">
                Google Calendar: Andere Kalender → Per URL · Outlook: Kalender hinzufügen → Aus dem Internet · Apple: Ablage → Kalenderabonnement
              </p>
              <p style="font-size:11px; color:rgba(244,63,94,0.5); margin:8px 0 0;">⚠ Diese URL ist persönlich — bitte nicht weitergeben.</p>
            </div>

            <!-- Organizer: Kalender-Link an alle senden -->
            <div v-if="isOrganizer" style="margin-top:14px; padding-top:14px; border-top:1px solid rgba(255,255,255,0.07);">
              <div v-if="calLinkSent" style="text-align:center; color:#10b981; font-size:13px; padding:6px 0;">
                ✓ Links werden versendet!
              </div>
              <button v-else @click="sendCalendarLinks" :disabled="calLinkSending"
                style="width:100%; padding:9px; border-radius:10px; border:1px solid rgba(255,255,255,0.15); background:transparent; color:rgba(255,255,255,0.7); cursor:pointer; font-size:12px; font-weight:600;">
                {{ calLinkSending ? 'Sende…' : `📨 Kalender-Link an alle ${participants.length} senden` }}
              </button>
            </div>
          </div>
```

- [ ] **Schritt 5: Uhrzeit-Felder in die "Termin bestätigt"-Karte einfügen**

Im `<template>`, die bestehende "Kalender-Einladung"-Karte (Zeilen ca. 671–682) erweitern. Den Inhalt dieser Karte nach dem bestehenden `<button @click="showCalendarModal = true"...>` einfügen:

```html
            <!-- Uhrzeit für Kalender setzen -->
            <div style="margin-top:16px; padding-top:14px; border-top:1px solid rgba(255,255,255,0.07);">
              <p style="font-size:11px; color:rgba(255,255,255,0.35); text-transform:uppercase; letter-spacing:.08em; margin-bottom:10px;">Uhrzeit im Kalender</p>
              <div style="display:grid; grid-template-columns:1fr 1fr; gap:8px; margin-bottom:10px;">
                <div>
                  <label style="font-size:11px; color:rgba(255,255,255,0.35); display:block; margin-bottom:4px;">Beginn</label>
                  <input type="time" v-model="eventStartTime"
                    style="width:100%; background:rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.12); border-radius:8px; padding:8px 10px; color:#fff; font-size:13px; outline:none; color-scheme:dark; box-sizing:border-box;" />
                </div>
                <div>
                  <label style="font-size:11px; color:rgba(255,255,255,0.35); display:block; margin-bottom:4px;">Ende</label>
                  <input type="time" v-model="eventEndTime"
                    style="width:100%; background:rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.12); border-radius:8px; padding:8px 10px; color:#fff; font-size:13px; outline:none; color-scheme:dark; box-sizing:border-box;" />
                </div>
              </div>
              <p style="font-size:11px; color:rgba(255,255,255,0.25); margin-bottom:10px;">Ohne Uhrzeit wird ein Ganztages-Termin im Kalender eingetragen.</p>
              <button @click="saveEventTime" :disabled="timeSaving"
                :style="{ width:'100%', padding:'8px', borderRadius:'10px', border:'none', cursor: timeSaving ? 'default' : 'pointer', fontWeight:600, fontSize:'12px', color:'#000', background: event.accent_color, opacity: timeSaving ? 0.7 : 1 }">
                {{ timeSaving ? 'Speichere…' : 'Uhrzeit speichern' }}
              </button>
            </div>
```

- [ ] **Schritt 6: Frontend bauen**

```bash
cd /docker/eventfinder && docker compose up -d --build frontend
sleep 15
docker logs eventfinder-frontend 2>&1 | tail -5
```

Erwartete Ausgabe: Kein Build-Fehler, Container `Up`.

- [ ] **Schritt 7: Manueller End-to-End-Test**

1. Browser öffnen → Event-Detailseite
2. Linke Spalte → "Kalender abonnieren" → "Meine Kalender-URL anzeigen" klicken → URL erscheint
3. "URL kopieren" klicken → URL in neuem Tab einfügen → `.ics` Datei wird gedownloadet (oder `BEGIN:VCALENDAR` erscheint)
4. Als Organizer: Uhrzeiten eingeben → "Uhrzeit speichern" → Feed nochmal abrufen → DTSTART mit Uhrzeit prüfen
5. Als Organizer: "Kalender-Link an alle X senden" → E-Mails werden verschickt (Worker-Log prüfen)

- [ ] **Schritt 8: Participant-Removal testen**

```bash
# Als Organizer: Teilnehmer entfernen
curl -s -X DELETE "http://localhost:58416/events/<EVENT_ID>/participants/<USER_ID>" \
  -H "Authorization: Bearer <ORGANIZER_TOKEN>"

# Dann calendar.ics mit dem Token des entfernten Users abrufen → muss 403 sein
curl -v "http://localhost:58416/events/<EVENT_ID>/calendar.ics?token=<REMOVED_USER_CALENDAR_TOKEN>"
```

Erwartete Ausgabe: HTTP 403.

- [ ] **Schritt 9: Commit**

```bash
cd /docker/eventfinder
git add frontend/src/views/EventDetails.vue
git commit -m "feat: add calendar subscribe UI, time fields, and organizer calendar-link push"
```

---

### Task 8: Final-Rebuild und Smoke-Test

- [ ] **Schritt 1: Vollständiger Rebuild**

```bash
cd /docker/eventfinder && docker compose down && docker compose up -d --build
sleep 20
docker ps --filter name=eventfinder --format "{{.Names}}\t{{.Status}}"
```

Alle 5 Container müssen `Up` sein.

- [ ] **Schritt 2: Migrations-Status prüfen**

```bash
docker exec eventfinder-backend bash -c "cd /app && alembic current"
```

Erwartete Ausgabe: `head` — d.h. alle Migrationen angewendet.

- [ ] **Schritt 3: Vollständiger API-Smoke-Test**

```bash
# 1. Calendar token holen
curl -s -X POST http://localhost:58416/auth/calendar-token \
  -H "Authorization: Bearer <TOKEN>" | python3 -m json.tool

# 2. Feed abrufen
curl -s "http://localhost:58416/events/<EVENT_ID>/calendar.ics?token=<CALENDAR_TOKEN>" | head -10

# 3. Feed mit falschem Token → 401
curl -s -o /dev/null -w "%{http_code}" "http://localhost:58416/events/<EVENT_ID>/calendar.ics?token=falscher_token"

# 4. PATCH Uhrzeit
curl -s -X PATCH "http://localhost:58416/events/<EVENT_ID>" \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"event_start_time": "14:00", "event_end_time": "18:00"}' | python3 -m json.tool | grep event_start

# 5. Feed nochmal → DTSTART mit Uhrzeit prüfen
curl -s "http://localhost:58416/events/<EVENT_ID>/calendar.ics?token=<CALENDAR_TOKEN>" | grep DTSTART
```

- [ ] **Schritt 4: Finaler Commit**

```bash
cd /docker/eventfinder
git add -A
git commit -m "feat: complete iCal subscription feed with voting summary, time support, and organizer push"
```
